<resources>

    <!-- Base application theme. -->
    <!--Toolbar Style-->
    <style name="Theme.Architecture.AppBar" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.Architecture.Toolbar" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Architecture.TransparentActionBar" parent="Theme.Architecture.AppBar">
        <item name="windowActionBar">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
